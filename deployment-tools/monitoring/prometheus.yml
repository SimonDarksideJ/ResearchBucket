global:
  scrape_interval: 15s

scrape_configs:
  - job_name: livekit
    static_configs:
      - targets:
          # Replace with the reachable hostname or IP of your LiveKit node.
          - livekit-host.example.com:6789
