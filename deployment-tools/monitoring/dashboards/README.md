# Grafana Dashboards

Place exported Grafana dashboard JSON files in this folder if you want Grafana to auto-provision them at startup.

The repository intentionally ships the provisioning path but does not hardcode a dashboard bundle that would drift from the actual metrics in use.
